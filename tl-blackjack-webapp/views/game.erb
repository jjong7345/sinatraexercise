<h4>Hi, <%= session[:player_name] %> </h4>

Your cards are:
<ul>
  <% session[:player_card].each do |card| %>
    <li><%= card.to_s %></li>
  <% end %>
</ul>
your total is <%= calculate_total(session[:player_card]) %>